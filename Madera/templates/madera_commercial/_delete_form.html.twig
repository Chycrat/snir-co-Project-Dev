<form method="post" action="{{ path('madera_commercial_delete', {'id': madera_commercial.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer ce commercial?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ madera_commercial.id) }}">
    <button class="btn btn-danger">Suppression commercial</button>
</form>
