<form method="post" action="{{ path('madera_projet_delete', {'id': madera_projet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ madera_projet.id) }}">
    <button class="btn btn-danger">Suppression projet</button>
</form>
