<form method="post" action="{{ path('madera_devis_delete', {'id': devis.getId() , 'idPlan' : plan.getId()}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer ce devis?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ devis.getId()) }}">
    <button class="btn btn-danger">Supression devis</button>
</form>
