<form method="post" action="{{ path('madera_devis_delete', {'id': madera_devi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ madera_devi.id) }}">
    <button class="btn">Delete</button>
</form>
