<form method="post" action="{{ path('madera_module_delete', {'id': madera_module.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer ce module?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ madera_module.id) }}">
    <button class="btn btn-danger">Suppression module</button>
</form>
